services:
  db:
    image: postgres:15
    container_name: bibliography_project_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bibliography_project
    ports:
      - "5432:5432"
    volumes:
      - ./sql:/docker-entrypoint-initdb.d